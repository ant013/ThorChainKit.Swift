# THR-135 — S2-01 Send Domain and Quote Contract

## Goal

Implement only the approved S2-01 public immutable send/quote domain and Kit
facade contract from architecture revision 10 at commit
`518835315a65996b9321665213adb0516503df65`. This plan is a new formalization
revision. The exact commit containing this revised plan and spec is the only
approval target; it is recorded in the Paperclip handoff and PR before approval.

The resulting package exposes reviewable native-RUNE values and a host-owned
signer capability without exposing private keys, raw transaction construction,
account/sequence/fee overrides, or upstream error data.

## Assumptions

- Sprint 1 `Kit`, `Address`, `Network`, lifecycle, Combine, and BigInt 5.3.0
  contracts remain authoritative.
- The iOS 13 library floor and UI-agnostic `Sources/ThorChainKit` boundary are
  unchanged.
- Implementation starts only after explicit approval of the final spec and
  this plan revision.
- Network preflight, protobuf, byte-level signing verification, journal,
  transport, durable pending/retry runtime, Example UI, host integration, and
  S2-02 remain out of scope. S2-01 owns public contracts and an in-memory
  contract fixture only.

## Acceptance criteria

1. The complete public `SendAmount` / `SendQuote` / `SigningRequest` /
   `SendSubmission` / `TransactionID` / `PendingTransaction` / `SendError`
   graph compiles as checked `Sendable` without public protobuf or host imports.
2. `Kit` is the only public behavior facade for quote, send, retry, and pending
   snapshots/publishers; no public initializer can forge authoritative quotes,
   signing requests, or nonempty `QuoteChanges` payloads.
3. Amount and fee values cross actors/tasks as canonical snapshots, not stored
   `BigUInt` instances; public accessors reconstruct independent values.
4. Quote identity is opaque, bound to the originating Kit/wallet namespace and
   lifecycle generation, expires at ten seconds, and is consumable once.
5. Local validation is deterministic and occurs after active-Kit admission but
   before endpoint, signer, QuoteStore, or journal work.
6. Public errors are deterministic, bounded, `Equatable`, `Sendable`, and
   redact URLs, credentials, wallet identifiers, public-key bytes, signatures,
   SignDoc/TxRaw bytes, and upstream response bodies.

## Analog conclusion

- Primary spine: current TronKit Kit/TransactionSender/TransactionManager
  ownership and send/pending composition only; its raw transaction authority is
  a rejected delta.
- Supporting roles: current Unstoppable SendNew handler split (`sendData` for
  review/preflight and `send` for authorization), current ThorChainKit Sprint 1
  public API test seam, and a greenfield immutable-domain delta.
- Rejected counterexample: EvmKit raw transaction plus signature submission and
  seed/private-key signer factories; those violate the host-neutral authority
  boundary.
- Required delta: move authoritative quote/review state into immutable
  ThorChainKit values with internal-only construction and an opaque one-use
  QuoteStore identity. Preserve Kit ownership and explicit review/send split.

## Implementation steps

### 1. Public immutable domain graph

- Owner: ThorChainSwiftEngineer
- Files: `Sources/ThorChainKit/Send/Domain/*`, `Send/Signer.swift`,
  `Send/TransactionID.swift`, `Send/PendingTransaction.swift` as needed by the
  approved package layout.
- Tests first: public-consumer compile probe; strict-concurrency `Sendable`
  probes; value round-trip tests above and below `UInt64`; memberwise-
  initializer visibility checks; explicit redacted debug projections for
  `SendQuote`, `SigningRequest`, and `PendingTransaction`.
- Acceptance: criteria 1 and 3.

### 2. Kit facade and quote identity

- Owner: ThorChainSwiftEngineer
- Files: `Sources/ThorChainKit/Core/Kit+Send.swift`,
  `Sources/ThorChainKit/Send/Internal/QuoteStore.swift`.
- Tests first: lifecycle precedence, stop/start generation invalidation, late
  H0 callback discard, ten-second expiry boundary, namespace binding, and
  sequential/concurrent one-use consumption. QuoteStore uses a 32-byte secure
  token, an injected monotonic clock, and an atomic consume linearization point.
  The facade snapshots optional retry fees before its first actor hop.
- Acceptance: criteria 2, 4, and 5; no transport or signer work on rejected
  local input. S2-01 proves only the admission boundary; durable
  journal/recovery/operation-hold behavior is handed to S2-05.

### 3. Stable sanitized errors

- Owner: ThorChainSwiftEngineer
- Files: `Sources/ThorChainKit/Send/Errors/SendError.swift` and only directly
  required supporting value files.
- Tests first: every public case and payload; empty `QuoteChanges` rejection;
  deterministic sorted descriptions; fixed allowlisted broadcast diagnostic
  categories; credential/URL/key/signature/SignDoc/TxRaw/response-body
  redaction canaries.
- Acceptance: criteria 1, 5, and 6.

### 4. Exact-head review and independent QA

- Owner: ThorChainCodeReviewer, then ThorChainQAEngineer
- Scope: exact PR head only; review against the S2-01 spec and this plan.
- Verification applies to the post-implementation PR head: `swift package
  dump-package`; focused domain, QuoteStore, public-API, and SendError tests;
  complete local `swift test`; temporary iOS consumer import/build. Each
  focused filter must show a nonzero discovered test count. PR evidence must
  cite one exact head SHA for CR and QA, `git diff --name-only`, local logs,
  `gh pr checks <PR>` with every required check conclusion, the CR approval
  comment/review artifact, build-only GitHub Actions conclusions, import/secret
  audits, and an explicit QA PASS. The docs-only architecture head intentionally
  has no `Package.swift`, so package commands are not run during formalization.
- Acceptance: all criteria mapped to tests, no S2-02 or later-slice files, and
  no secrets or host/UI imports.

## Explicit non-goals

No preflight implementation, protobuf codec, byte-level signing verification,
journal, durable pending/retry runtime, transport, Example UI, host adapter,
Vultisig integration, or S2-02 work. `SigningRequestFactory` is a later-slice
byte-consistency responsibility; S2-01 only defines the immutable public
request shape and internal construction precondition.

## Verification commands

```text
swift package dump-package
swift test --filter SendQuoteTests
swift test --filter QuoteStoreTests
swift test --filter SendErrorTests
swift test --filter SendPublicApiTests
swift test
xcodebuild -project <temporary-consumer-project> -scheme <consumer-scheme> -destination 'platform=iOS Simulator,id=<approved-udid>' build
```

The exact consumer project, scheme, and simulator destination must be recorded
in the implementation PR; placeholders are not evidence. S2-03 owns SignDoc
codec/byte-consistency tests; S2-04 owns signer binding and signature
verification tests.

The Gimle reliability report for this revision is generated under
`docs/reports/gimle/` and records the missing ThorChainKit project mapping,
stale Unstoppable index identity, and direct current-tree fallbacks.
