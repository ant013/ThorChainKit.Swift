# THR-135 — S2-01 Send Domain and Quote Contract

## Goal

Implement only the approved S2-01 public immutable send/quote domain and Kit
facade contract from architecture revision 10 at commit
`518835315a65996b9321665213adb0516503df65`. This plan is a new formalization
revision. The exact commit containing this revised plan and spec is the only
approval target; it is recorded in the Paperclip handoff and PR before approval.

The resulting package exposes reviewable native-RUNE values and a host-owned
signer capability without exposing private keys, raw transaction construction,
account/sequence/fee overrides, or upstream error data.

## Assumptions

- Sprint 1 `Kit`, `Address`, `Network`, lifecycle, Combine, and the package's
  declared BigInt `5.0.0` floor / committed default `5.7.0` lock remain
  authoritative; there is no BigInt `5.3.0` assumption.
- The iOS 13 library floor and UI-agnostic `Sources/ThorChainKit` boundary are
  unchanged.
- Implementation starts only after explicit approval of the final spec and
  this plan revision.
- Network preflight, protobuf, byte-level signing verification, journal,
  transport, durable pending/retry runtime, Example UI, host integration, and
  S2-02 remain out of scope. S2-01 owns public contracts and an in-memory
  contract fixture only.

## Acceptance criteria

The plan uses the spec's single canonical list: S2-01-A1 public graph,
S2-01-A2 immutable snapshots, S2-01-A3 facade/composition,
S2-01-A4 fail-closed deferred engines, S2-01-A5 quote lifecycle,
S2-01-A6 validation/errors, S2-01-A7 reflection/pending ordering, and
S2-01-A8 local verification. No second acceptance list is allowed.

## Executable acceptance inventory

| Criterion | Focused test or harness | Required proof |
| --- | --- | --- |
| S2-01-A1 | `SendPublicApiTests`; `Scripts/verify-s2-01-public-surface.sh` | External consumer must fail to construct `SendQuote`, `SigningRequest`, and `TransactionID` from memberwise initializers, `QuoteChanges(validating:)`, `.quoteChanged([])`, `QuoteAuthorityEnvelope`, or `SendQuote.internalAuthorityRecord`; source guards reject UIKit/SwiftUI, protobuf, raw transaction, secret, and override fields. |
| S2-01-A2 | `SendValueTests`; `Scripts/verify-s2-01-concurrency.sh --dependency 5.7.0` and `--dependency 5.0.0` | Boundary values above `UInt64`, optional retry-fee snapshot-before-hop, checked `Sendable`, and expected failure for a stored `BigUInt` control under Swift 5 complete concurrency. |
| S2-01-A3 | `KitCompositionTests`, `LifecycleCommandBridgeTests` | Both factory paths wire one `SendRuntime` and a distinct per-instance authority ID; successful start activates, successful stop invalidates, and failed close revokes the retained active generation. |
| S2-01-A4 | `SendFacadeAdmissionTests` | Inactive admission precedes validation; invalid active input and valid deferred operations return their stable errors with zero QuoteStore, signer, journal, endpoint, and publisher-spy calls. |
| S2-01-A5 | `QuoteStoreTests`, `LifecycleCommandBridgeTests` | 9.999/10.000-second boundaries, atomic one-use consume, two same-wallet/network Kits, failed-close late H0 callback, rapid restart, tombstones, and deliberate record/projection mismatch all fail closed as specified. |
| S2-01-A6 | `SendErrorTests` | Every finite case and payload is constructed; descriptions are deterministic, bounded, allowlisted, and sanitized. |
| S2-01-A7 | `SendReflectionTests`, `PendingTransactionTests` | `Mirror(reflecting:)`, `String(reflecting:)`, and `dump` omit authority record/token and all sensitive bytes/text; pending status semantics and `(createdAt, transactionId.hash)` ordering are asserted. |
| S2-01-A8 | `Scripts/verify-s2-01-deployment-floor.sh`; `Scripts/verify-s2-01-concurrency.sh`; `Scripts/verify-bigint-floor.sh` | iOS 13 `arm64-apple-ios13.0` build/import guard passes; concurrency probes pass at both dependency identities; floor command runs with `THORCHAIN_SIMULATOR_UDID=<selected-runtime-udid>` and records the UUID plus test discovery. |

Every row must emit a nonzero focused-test discovery count or a passing compile-negative/static harness result; the implementation and QA evidence must cite the row ID and exact head.

## Analog conclusion

- Primary spine: current Unstoppable SendNew handler split (`sendData` for
  review/preflight and `send` for authorization), typed validation data, and
  ten-second expiration.
- Supporting roles: current Unstoppable `SendHandlerFactory` composition,
  current TronKit Kit/TransactionSender/TransactionManager ownership boundary
  and pending composition, current ThorChainKit Sprint 1 public API test seam,
  and a greenfield immutable-domain delta. TronKit raw transaction authority is
  a rejected delta, not the S2-01 primary.
- Rejected counterexample: EvmKit raw transaction plus signature submission and
  seed/private-key signer factories; those violate the host-neutral authority
  boundary.
- Required delta: move authoritative quote/review state into immutable
  ThorChainKit values with internal-only construction and an opaque one-use
  QuoteStore identity; add the stored `SendRuntime` dependency through
  `KitDependencies`, the public `Kit.instance` and `Kit.fixture` entry points
  implemented in `Core/KitFactory.swift`, and bridge activation/invalidation
  from `LifecycleCommandBridge` at `LifecycleGate`. Preserve Kit ownership and
  explicit review/send split.

## Implementation steps

### 1. Public immutable domain graph

- Owner: ThorChainSwiftEngineer
- Files: `Sources/ThorChainKit/Send/Domain/*`, `Send/Signer.swift`,
  `Send/TransactionID.swift`, `Send/PendingTransaction.swift` as needed by the
  approved package layout.
- Tests first: public-consumer compile probe; strict-concurrency `Sendable`
  probes; value round-trip tests above and below `UInt64`; memberwise-
  initializer visibility checks; explicit redacted debug projections for
  `SendQuote`, `SigningRequest`, and `PendingTransaction`.
- Acceptance: S2-01-A1 and S2-01-A2.

### 2. Kit facade, composition, and quote identity

- Owner: ThorChainSwiftEngineer
- Files: `Sources/ThorChainKit/Core/Kit+Send.swift`,
  `Sources/ThorChainKit/Core/Kit.swift`,
  `Sources/ThorChainKit/Core/KitDependencies.swift`,
  `Sources/ThorChainKit/Core/KitFactory.swift`,
  `Sources/ThorChainKit/Sync/LifecycleCommandBridge.swift`, and
  `Sources/ThorChainKit/Send/Internal/QuoteStore.swift`.
- Tests first: lifecycle precedence, stop/start generation invalidation, late
  H0 callback discard, failed-close revocation, ten-second expiry boundary,
  per-instance authority binding, canonical record/projection equality, and
  sequential/concurrent one-use consumption. QuoteStore uses a 32-byte secure
  token with at most eight generation attempts, an injected monotonic clock,
  and an atomic consume linearization point. Its private authority envelope is
  checked in per-instance client ID, generation, deadline, and active/consumed
  order; terminal tombstones are retained only through the original deadline.
  `SendQuote` carries one canonical authority record in private stored fields
  through its internal accessor; the store compares that record for exact
  equality before consume and later signing uses the stored record rather than
  reconstructing it from public review values;
  after cleanup, an originating quote still reaches the deadline check, while
  an own unexpired quote missing from the store fails closed as
  `operationUnavailable`.
  The facade snapshots optional retry fees before its first actor hop. Lifecycle
  admission precedes local validation: invalid local input returns its stable
  validation error with zero dependency-spy calls, and only locally valid
  admitted calls fail with `operationUnavailable` and zero dependency-spy calls
  until later engines exist; pending replays `[]` with `.degraded` and no storage
  work.
- Acceptance: S2-01-A3, A4, and A5; no transport or signer work on rejected local
  input. S2-01 proves only the admission boundary; durable
  journal/recovery/operation-hold behavior is handed to S2-05.

### 3. Stable sanitized errors

- Owner: ThorChainSwiftEngineer
- Files: `Sources/ThorChainKit/Send/Errors/SendError.swift` and only directly
  required supporting value files.
- Tests first: every public case and payload; empty `QuoteChanges` rejection;
  deterministic sorted descriptions; fixed allowlisted broadcast codespace and
  diagnostic categories; credential/URL/key/signature/SignDoc/TxRaw/raw-
  codespace/response-body redaction canaries through debug and reflection paths.
- Acceptance: S2-01-A1, A6, and A7.

### 4. Exact-head review and independent QA

- Owner: ThorChainCodeReviewer, then ThorChainQAEngineer
- Scope: exact PR head only; review against the S2-01 spec and this plan.
- Verification applies to the post-implementation PR head: pin the PR
  `headRefOid` and `baseRefOid` from `gh pr view <PR> --json headRefOid,baseRefOid`
  before collecting evidence, refuse a changed head, and compare exactly with
  `git diff --name-only <baseRefOid>...<headRefOid>`; then run `swift package
  dump-package`; each named focused test filter; complete local `swift test`;
  `bash Scripts/verify-s2-01-public-surface.sh` with expected compile-negative
  probes; `bash Scripts/verify-s2-01-concurrency.sh --dependency 5.7.0` and
  `--dependency 5.0.0`; `bash Scripts/verify-s2-01-deployment-floor.sh`; and
  `THORCHAIN_SIMULATOR_UDID=<selected-runtime-udid> bash Scripts/verify-bigint-floor.sh`. Each focused
  filter must show a nonzero discovered test count. PR evidence must cite one
  exact pinned base/head SHA pair for CR and QA, the exact base...head file diff,
  local logs,
  `gh pr checks <PR>` with every required check conclusion, the CR approval
  comment/review artifact, build-only GitHub Actions conclusions, import,
  secret, and reflection audits, and an explicit QA PASS. The current exact
  worktree already contains `Package.swift`; package commands apply to the
  implementation head, while GitHub Actions remain build-only.
- Acceptance: S2-01-A1 through A8 mapped to named tests/harnesses, no S2-02 or
  later-slice files, and no secrets or host/UI imports.

## Explicit non-goals

No preflight implementation, protobuf codec, byte-level signing verification,
journal, durable pending/retry runtime, transport, Example UI, host adapter,
Vultisig integration, or S2-02 work. `SigningRequestFactory` is a later-slice
byte-consistency responsibility; S2-01 only defines the immutable public
request shape and internal construction precondition.

## Verification commands

```text
swift package dump-package
swift test --filter ThorChainKitTests.SendQuoteTests
swift test --filter ThorChainKitTests.QuoteStoreTests
swift test --filter ThorChainKitTests.SendErrorTests
swift test --filter ThorChainKitTests.SendPublicApiTests
swift test
bash Scripts/verify-s2-01-public-surface.sh
bash Scripts/verify-s2-01-concurrency.sh --dependency 5.7.0
bash Scripts/verify-s2-01-concurrency.sh --dependency 5.0.0
bash Scripts/verify-s2-01-deployment-floor.sh
THORCHAIN_SIMULATOR_UDID=<selected-runtime-udid> bash Scripts/verify-bigint-floor.sh
```

The public-surface harness compiles a temporary external consumer and asserts
expected failures for memberwise `SendQuote`, `SigningRequest`, and
`TransactionID` construction, internal `QuoteChanges` construction,
`.quoteChanged([])`, `QuoteAuthorityEnvelope`, and
`SendQuote.internalAuthorityRecord`; it also runs runtime reflection/dump
redaction checks. The concurrency harness
compiles the actual graph with `-swift-version 5 -strict-concurrency=complete
-warnings-as-errors` and asserts that a stored-`BigUInt` control fails. Each
focused command must report a nonzero discovered test count, and the floor
command must record `THORCHAIN_SIMULATOR_UDID` plus the default and exact-floor
dependency identities. S2-03 owns SignDoc codec/byte-consistency tests; S2-04
owns signer binding and signature verification tests.

The Gimle reliability report for this revision is generated under
`docs/reports/gimle/` and records the missing ThorChainKit project mapping,
stale Unstoppable index identity, and direct current-tree fallbacks.
