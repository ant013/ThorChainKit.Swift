# THR-138 — native RUNE sync correction plan

Plan source of truth: [THR-138 correction spec](../../specs/sprint-01-foundation/THR-138-s1-07-correction.md).

## Step 1 — Correct the verified account-absence contract

**Owner:** ThorChainSwiftEngineer after explicit design approval.

**Dependencies:** This step starts only after CodeReviewer adversarial review
accepts the spec and the user explicitly approves the exact design revision.

**Affected paths:**

- `Sources/ThorChainKit/Network/LiveThorNodeClient.swift`
- `Tests/ThorChainKitTests/LiveThorNodeClientS1_04Tests.swift`

**Acceptance:** The account reader accepts both verified address-specific
absence messages with code `5` and empty details; generic/foreign/malformed
404 bodies still throw the typed account HTTP error; balance handling is
unchanged.

**Verification:** Focused regression test fails before the fix for the short
body and passes after it; the full directly affected ThorChainKit test file
passes; diff contains only the approved predicate/test delta.

## Step 2 — Local UW verification and live-smoke

**Owner:** ThorChainQAEngineer after the engineer’s exact PR head is posted.

**Dependencies:** Step 1 and the exact local MarketKit/ThorChainKit checkouts;
the separate provider audit remains an acceptance dependency, not a reason to
select another provider here.

**Affected paths:** No source change is authorized by this step.

**Acceptance:** Relevant WalletCore/ThorChain tests and the local Development
build pass; the MacBook live-smoke reproduces the short Liquify response and
then reaches the expected no-funds native RUNE behavior after the correction;
no GitHub Actions or Maestro is used.

**Verification:** QA posts concrete local command output, app/device/OS,
endpoint family, response shape, sync state, height, and exact RUNE projection
with no secrets or mnemonic material.

## Step 3 — CTO merge gate

**Owner:** ThorChainCTO.

**Dependencies:** CodeReviewer APPROVE on the exact head, QA PASS on the exact
head, required local checks green, and the provider-audit acceptance gate.

**Acceptance:** No Sprint 2 child resumes until this correction is accepted and
the provider audit is attached.
