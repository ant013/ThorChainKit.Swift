# THR-159 S2-06 code-review evidence — 2026-07-26

## Review identity

- Reviewed head: `74aaca63cf4448dd00d2f1800289b3b2049857bb`
- Base: `fe290029272dc71a37917a7869ea3bae83fd8e6f`
- Scope: package-owned ThorChainKit iOS Example S2-06 only
- Review budget: discovery `2/2`; closure `5/5`
- Repository changes: this report only; no product implementation changes

## Evidence substrate

Codebase-memory project `Users-ant013-Data-AI-thorchain` was queried first and
reported `status=ready`, `199464` nodes, and `904321` edges. Its bounded search
timed out after 300 seconds; no load-bearing decision used that result.
Serena was unavailable in this reviewer workspace. Current-tree Git reads,
targeted `rg`, and an extracted exact-head worktree were used as the independent
fallback. Gimle trust is `RED`/unavailable for this review; the fallback is
explicit rather than silently treated as Gimle evidence.

Required read-only architecture, security, and verification lanes independently
returned NO-GO/REJECT. Their anchors were reconciled against the exact head.

## Verification performed

- `git diff --check fe290029...74aaca63` — passed.
- Extracted-head `Scripts/audit-example-target-graph.sh` — passed.
- Extracted-head `Scripts/test-run-maestro-s2-06.sh` — passed its static checks,
  but those checks do not prove the required runtime behavior.
- Extracted-head Python compilation and shell syntax checks — passed.
- Mutated the second manifest flow to remove its required action: the validator
  still passed (`Scripts/validate-s2-06-manifest.py:30-48`). Mutated the YAML to
  remove the `send.confirm.button` action: it also passed. This reproduces the
  false-green guard.

## High/critical findings

1. **Critical — the authoritative runner cannot complete its own artifact scan.**
   `Scripts/run-maestro-s2-06.sh:55-56` writes `tracked-list` under `mktemp`,
   while `Scripts/scan-s1-01-artifacts.swift:117-120` requires that file to be
   contained by the artifact root. Any run reaching that step fails.

2. **High — fixture transcript is not strict ordered origin/method/path/query/body.**
   `iOS Example/Sources/Signing/FixtureTransport.swift:31-58` permits the first
   three probes in any order, accepts arbitrary queries when `query == nil`, and
   checks only body presence. The broadcast path separately accepts a JSON body
   with extra fields (`:121-125`). This violates the explicit transcript
   acceptance criterion.

3. **High — manifest and flow guards do not enforce the approved action/assertion
   matrix.** `Scripts/validate-s2-06-manifest.py:30-48` checks non-empty arrays
   and a small substring allowlist, while
   `Scripts/test-run-maestro-s2-06.sh:12-26` mutates only one manifest entry and
   one YAML assertion. The reproduced action mutants above pass.

4. **High — runtime UI evidence is incomplete and can be impossible at the final
   snapshot.** `Scripts/verify-s2-06-artifacts.py:41-55` requires only four IDs,
   omits most fixed IDs, and has no unlisted-node negative mutation. The runner
   captures one hierarchy after all five flows (`run-maestro-s2-06.sh:27-45`),
   while `send.confirm.button` exists only when a review is present
   (`iOS Example/Sources/Send/SendView.swift:24-31` and
   `SendReviewView.swift:18-22`). The final restart-pending flow does not
   guarantee that review state, so the gate is not a valid all-screen ID proof.

5. **High — exact-head/clean-input provenance is not fail-closed.**
   `Scripts/verify-s2-06-artifacts.py:73-79` compares only the artifact directory
   to the current HEAD and ignores untracked dirty files; it has no approved
   expected input digest set. The optional manifest is created after the run
   (`:105-128`), so wrong-head/modified-input evidence can self-authorize.

6. **Critical — controlled LIVE safety is absent.**
   `iOS Example/Sources/Signing/LiveSendSession.swift:53-55` constructs the normal
   `Kit.instance` without a broadcast-denying transport or count-only send guard,
   while the shared confirmation path calls `Kit.send`
   (`iOS Example/Sources/Send/SendReviewView.swift:17-22`). This does not satisfy
   the approved read-only LIVE observation boundary.

7. **High — retry/restart flows do not prove the required terminal semantics.**
   The fixture transport returns a generic successful response on retry
   (`FixtureTransport.swift:133-136`), while the UI hardcodes `sdk/19` in the
   pending projection (`PendingView.swift:20-30`). The retry YAML does not assert
   terminal wording, disabled retry/replacement, or restart persistence
   (`.maestro/sprint-02/send-retry.yaml:26-38`); restart relaunches without the
   scenario argument (`send-restart-pending.yaml:19-25`), which defaults to the
   quote-review scenario (`ExampleRuntime.swift:230-234`).

## Disposition

`REVISE` / request changes. The exact head is not ready for code-review approval
or QA handoff. The engineer must correct the high/critical findings, rerun the
required local checks and exact-head evidence, then request a fresh targeted
closure review. Medium concurrency minimality observations remain backlog and
do not affect this disposition.
