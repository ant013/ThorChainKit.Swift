{
  "version": 1,
  "flows": [
    {"flow": "send-quote-review", "scenarioArgument": "--example-scenario=send-quote-review", "actions": ["launch send-quote-review", "enter amount", "enter memo", "quote", "advance exact deadline"], "assertions": ["review recipient", "review amount", "review memo", "review native fee", "review total", "review height", "absolute expiry", "expiry disables confirm", "zero signer calls"]},
    {"flow": "send-checktx-accepted", "scenarioArgument": "--example-scenario=send-checktx-accepted", "actions": ["launch send-checktx-accepted", "quote", "confirm"], "assertions": ["CheckTx accepted — not confirmed", "local hash"]},
    {"flow": "send-unknown", "scenarioArgument": "--example-scenario=send-unknown", "actions": ["launch send-unknown", "quote", "confirm"], "assertions": ["Unknown — retry available", "canonical local hash", "retry available"]},
    {"flow": "send-retry", "scenarioArgument": "--example-scenario=send-retry", "actions": ["launch send-retry", "restore unknown pending", "acknowledge current fee", "retry"], "assertions": ["same hash", "same signer count", "previous native fee", "current native fee", "exact current-fee acknowledgement", "sdk/19", "signer count unchanged"]},
    {"flow": "send-restart-pending", "scenarioArgument": "--example-scenario=send-restart-pending", "actions": ["launch send-restart-pending", "create pending", "relaunch same namespace"], "assertions": ["pending before relaunch", "pending after relaunch", "same namespace", "empty versus unavailable"]}
  ]
}
